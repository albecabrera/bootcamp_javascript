// bucle while
let contador = 0;

while (contador < 5) {
    console.log(`Counter is at: ${contador}`);
    contador++;
} // Counter ist at:0, Counter ist at:1, Counter ist at:2, Counter ist at:3, Counter ist at:4, 

// Otro ejemplo
let number = 10;

while (number > 0) {
    console.log(`Number is: ${number}`);
    number--;
}